const { getConnection } = require('./../database/database')

const newOrder = async (req = request, res = response) => {
    const {id_user, id_shoes} = req.body
    const neworder = {
        
    }
}